<%= @user.email_address %> just signed up as "<%= @account.name %>".

The account is pending until you approve it. Review and approve:

<%= @approval_url %>

Approving sends them a welcome email with getting-started instructions. This link works for 30 days; after that, approve from the console.
